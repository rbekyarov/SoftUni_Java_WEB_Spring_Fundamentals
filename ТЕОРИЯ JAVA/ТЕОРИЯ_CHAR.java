import java.util.Scanner;
import java.math.BigInteger;
public class ТЕОРИЯ_CHAR {
    public static void main(String[] args) {
/**********************************************************  */
        /*(Четене от конзола) */
        /*char text = scanner.nextLine().charAt(0);*/

/**********************************************************  */
        /*(Вземане на символ от стринг) */
     /*   String text = "SoftUni";
        int length = text.length();

        String text = "SoftUni";
        char letter = text.charAt(4);*/
/**********************************************************  */
        /*(Вземане на символ чрез цикъл и проверка с switch) */
       /* for (int i = 0; i < input.length(); i++) {
            switch (input.charAt(i))
            case 'a': sum += 1; break;
            case 'e': sum += 2; break;*/
/**********************************************************  */
        /*(Вземане на символ чрез цикъл) */
/*
        for (int i = 0; i < input.length(); i++) {
            char letter = input.charAt(i);
            System.out.println(letter);
        }*/
/**********************************************************  */
        /*(Четене на символ от конзолата чрез цикъл) */
        /*for (int j = 0; j < finish; j++) {
            char letter = (command.charAt(j));*/
/**********************************************************  */
        /*(сравняване на символи) */
        /*word.charAt(0) == 'a' || word.charAt(0) == 'e'*/
/**********************************************************  */
        /*(Вземане на символ чрез цикъл и обръщане на всички в малки букви) */
       /* for (int i = 0; i < text.length(); i++) {
            char letter = text.charAt(1);
            switch (Character. to LowerCase(letter)) {
                case 'a': totalSum+=1; break;
                case 'e': totalSum+=2; break; */

/**********************************************************  */
        /*(ПРОВЕРКА ДАЛИ ДАДЕН CHAR E БУКВА ИЛИ ЦИФРА) */
       /* for (int i = 0; i < password.length(); i++) {
            char symbol = password.charAt(i);
            if (Character.isDigit(symbol))
            if (Character.isLetter(symbol))
             */



    }
}